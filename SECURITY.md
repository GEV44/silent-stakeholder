# Security

Report security issues privately to the project owner. Do not include credentials or raw user data in
an issue.

Credentials must be supplied via environment variables. A GitHub token needs public-repository read
access only. Google credentials should use the least-privileged project and should be rotated
immediately if pasted into chat, logs, source, or screenshots.

Before publishing, scan the repository for secrets and review cached outputs for user text.

